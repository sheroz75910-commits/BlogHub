 <div class="max-w-5xl mx-auto space-y-8 mr-0">

    <!-- PAGE TITLE -->
    <div class="flex justify-between items-center">
      <h1 class="text-3xl font-bold text-gray-800">RPM Management</h1>
      
    </div>

    <!-- RPM TABLE -->
    <div class="bg-white rounded-2xl shadow overflow-hidden">
      <div class="px-6 py-4 border-b font-semibold text-gray-700">
        RPM Groups
      </div>

      <div class="overflow-x-auto">
        <table class="w-full text-sm">
          <thead class="bg-gray-100 text-gray-600 uppercase text-xs">
            <tr>
              <th class="px-6 py-3 text-left">RPM Level</th>
              <th class="px-6 py-3 text-left">Price / 1000 Views</th>
              <th class="px-6 py-3 text-left">Status</th>
            </tr>
          </thead>

          <tbody class="divide-y">

       <% if (allRpmData && allRpmData.length > 0) { %>

  <% allRpmData.forEach(rpm => { %>

    <tr class="hover:bg-gray-50">
      <td class="px-6 py-4">
        <span class=" px-3 py-1 rounded-full text-xs font-semibold <%= rpm.name === 'High' ? 'bg-green-100 text-green-700' : rpm.name === 'Medium' ? 'bg-blue-100 text-blue-700' : rpm.name === 'Normal' ? 'bg-yellow-100 text-yellow-700' :  'bg-red-100 text-red-700'%>">
          <%= rpm.name  %>
        </span>
      </td>

      <td class="px-6 py-4">$<%= rpm.rate_per_1000 %></td>

      <td class="px-6 py-4  font-semibold <%=  rpm.status === 'Active' ? 'text-green-600' : 'text-red-600'%>">
        <%= rpm.status %>
      </td>
    </tr>

  <% }); %>

<% } else { %>
  <tr>
    <td colspan="3" class="text-center py-6 text-gray-500">
      No data yet
    </td>
  </tr>
<% } %>


            <!-- <tr class="hover:bg-gray-50">
              <td class="px-6 py-4">
                <span class="bg-blue-100 text-blue-700 px-3 py-1 rounded-full text-xs font-semibold">
                  Medium
                </span>
              </td>
              <td class="px-6 py-4">$5</td>
              <td class="px-6 py-4 text-green-600 font-semibold">Active</td>
             
            </tr> -->

            <!-- <tr class="hover:bg-gray-50">
              <td class="px-6 py-4">
                <span class="bg-yellow-100 text-yellow-700 px-3 py-1 rounded-full text-xs font-semibold">
                  Normal
                </span>
              </td>
              <td class="px-6 py-4">$4</td>
              <td class="px-6 py-4 text-green-600 font-semibold">Active</td>
             
            </tr> -->

            <!-- <tr class="hover:bg-gray-50">
              <td class="px-6 py-4">
                <span class="bg-red-100 text-red-700 px-3 py-1 rounded-full text-xs font-semibold">
                  Low
                </span>
              </td>
              <td class="px-6 py-4">$3</td>
              <td class="px-6 py-4 text-red-600 font-semibold">Inactive</td>
            </tr> -->
          </tbody>
        </table>
      </div>
    </div>

    <!-- FORM -->
    <section id="formBox" class="bg-white rounded-2xl shadow p-8 max-w-xl mx-auto">
      <h2 class="text-2xl font-bold mb-6 text-center text-gray-800">
        Add / Update RPM Group
      </h2>

      <form action="/api/admin-RPM/submit" method="POST" class="space-y-5">

        <div>
          <label class="block mb-1 font-medium text-gray-700">RPM Level</label>
          <select name="name" class="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-blue-400">
            <option value="" disabled selected >select Option</option>
            <option value="High">High</option>
            <option value="Medium">Medium</option>
            <option value="Normal">Normal</option>
            <option value="Low">Low</option>
          </select>
        </div>

        <div>
          <label class="block mb-1 font-medium text-gray-700">
            Rate per 1000 Views ($)
          </label>
          <input type="number" step="0.01" name="rate"
            placeholder="Example: 3.50"
            class="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-blue-400">
        </div>

        <div>
          <label class="block mb-1 font-medium text-gray-700">Status</label>
          <select name="status" class="w-full px-4 py-2 border rounded-lg focus:ring-2 focus:ring-blue-400">
            <option value="" disabled selected >Select Status</option>
            <option value="Active">Active</option>
            <option value="Inactive">Inactive</option>
          </select>
        </div>

        <button
          class="w-full bg-blue-600 text-white py-2 rounded-xl hover:bg-blue-700 font-semibold">
          Save RPM Group
        </button>
      </form>
    </section>

  </div>